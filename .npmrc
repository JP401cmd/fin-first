# legacy-peer-deps: @vercel/speed-insights@2 declareert optionele peers
# (svelte/vue/nuxt) waarvan npm's resolver de peer-keten toch probeert op te
# lossen en dan botst met onze vite 7 (vitest-toolchain) — ERESOLVE zonder dat
# er echt een conflict is (wij gebruiken geen svelte/vue). Geldt voor lokale
# installs en Vercel-builds, zodat beide identiek resolven.
legacy-peer-deps=true
