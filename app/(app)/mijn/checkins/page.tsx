/**
 * /mijn/checkins — tijdlijn van alle maandelijkse geldcheck-ins.
 *
 * Hergebruikt de bestaande check-in-historie (Client Component) onder de
 * canonieke /mijn-tak, zodat de gebruiker zijn check-ins via het profiel
 * terugvindt. Zelfde re-export-patroon als /mijn/profiel.
 */
export { default } from '../../core/checkin/historie/page'
